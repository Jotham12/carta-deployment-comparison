apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cephfs-images-pvc
  namespace: carta
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: microk8s-hostpath
  resources:
    requests:
      storage: 1Gi
